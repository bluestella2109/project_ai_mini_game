(()=>{const clock=document.getElementById("clock");const tick=()=>{if(clock)clock.textContent=new Date().toLocaleTimeString("ja-JP",{hour12:false})};tick();setInterval(tick,1000);const c=document.getElementById("codeRain");if(!c)return;const x=c.getContext("2d"),chars="01ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789<>[]{}//::";let cols=[],f=13;function size(){const d=Math.min(devicePixelRatio||1,2);c.width=innerWidth*d;c.height=innerHeight*d;c.style.width=innerWidth+"px";c.style.height=innerHeight+"px";x.setTransform(d,0,0,d,0,0);cols=Array(Math.ceil(innerWidth/f)).fill(0).map(()=>Math.random()*innerHeight/f)}function draw(){x.fillStyle="rgba(3,7,18,.10)";x.fillRect(0,0,innerWidth,innerHeight);x.font=f+"px ui-monospace,monospace";cols.forEach((y,i)=>{x.fillStyle=Math.random()>.82?"rgba(0,229,255,.34)":"rgba(30,90,140,.18)";x.fillText(chars[(Math.random()*chars.length)|0],i*f,y*f);if(y*f>innerHeight&&Math.random()>.975)cols[i]=0;cols[i]+=Math.random()*.35+.15});requestAnimationFrame(draw)}addEventListener("resize",size,{passive:true});size();draw()})();
